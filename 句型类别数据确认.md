# ✅ 句型类别数据确认

## 📝 数据位置

所有句型数据都在 `Word.swift` 的 `getDefaultWords()` 函数中：

### 简单句型（Phrases）- 免费版

**位置**：`Word.swift` 第 495-516 行

**代码**：
```swift
// 简单句型 (Phrases) - 免费
let phrases = [
    ("Hello", "你好", "👋"),
    ("Thank you", "謝謝", "🙏"),
    ("Good morning", "早安", "🌅"),
    ("Good night", "晚安", "🌙"),
    ("I love you", "我愛你", "❤️"),
    ("Goodbye", "再見", "👋"),
    ("Yes", "是的", "✅"),
    ("No", "不是", "❌"),
    ("Please", "請", "🙏"),
    ("Sorry", "對不起", "😔")
]

for (eng, chi, img) in phrases {
    words.append(Word(
        english: eng,
        chinese: chi,
        category: .phrases,  // ← 关键：使用 .phrases 枚举值
        imageName: img
    ))
}
```

**数量**：10 个句子  
**状态**：✅ 已添加

---

### 进阶句型（Advanced Phrases）- VIP 专属

**位置**：`Word.swift` 第 518-562 行

**代码**：
```swift
// 进阶句型 (Advanced Phrases) - VIP 专属
let advancedPhrases = [
    ("How are you?", "你好嗎？", "😊"),
    ("I am fine", "我很好", "👍"),
    ("What is this?", "這是什麼？", "❓"),
    ("I am hungry", "我餓了", "😋"),
    ("I am thirsty", "我渴了", "🥤"),
    ("I want to play", "我想玩", "🎮"),
    ("I need help", "我需要幫忙", "🆘"),
    ("Can I have it?", "我可以拿嗎？", "🙋"),
    ("Let's go", "我們走吧", "🚶"),
    ("Come here", "過來這裡", "👈"),
    ("Sit down", "坐下", "💺"),
    ("Stand up", "站起來", "🧍"),
    ("Be quiet", "安靜", "🤫"),
    ("Look at me", "看我", "👀"),
    ("Listen to me", "聽我說", "👂"),
    ("I don't know", "我不知道", "🤷"),
    ("Where is it?", "在哪裡？", "🔍"),
    ("I like it", "我喜歡", "😍"),
    ("Very good", "非常好", "👏"),
    ("See you later", "待會見", "🤚")
]

for (eng, chi, img) in advancedPhrases {
    words.append(Word(
        english: eng,
        chinese: chi,
        category: .advancedPhrases,  // ← 关键：使用 .advancedPhrases 枚举值
        imageName: img
    ))
}
```

**数量**：20 个句子  
**状态**：✅ 已添加

---

## 🔍 类别枚举确认

**位置**：`WordCategory.swift`

```swift
enum WordCategory: String, CaseIterable, Identifiable {
    // 免费类别
    case alphabet = "Alphabet"
    case animals = "Animals"
    case colors = "Colors"
    case numbers = "Numbers"
    case fruits = "Fruits"
    case phrases = "Phrases"  // ✅ 已添加
    
    // VIP 专属类别
    case advancedPhrases = "Advanced Phrases"  // ✅ 已添加
    case family = "Family"
    // ... 其他 VIP 类别
}
```

**状态**：✅ 枚举已正确定义

---

## 🎨 UI 配置确认

### 中文名称

```swift
var chineseName: String {
    switch self {
    // ...
    case .phrases: return "簡單句型"  // ✅
    case .advancedPhrases: return "進階句型"  // ✅
    // ...
    }
}
```

### 图标

```swift
var icon: String {
    switch self {
    // ...
    case .phrases: return "text.bubble.fill"  // ✅
    case .advancedPhrases: return "text.bubble.fill"  // ✅
    // ...
    }
}
```

### 颜色

```swift
var themeColor: Color {
    switch self {
    // ...
    case .phrases: return Color(hex: "9B59B6")  // 紫色 ✅
    case .advancedPhrases: return Color(hex: "8E44AD")  // 深紫 ✅
    // ...
    }
}
```

### VIP 权限

```swift
var isVIPOnly: Bool {
    switch self {
    case .advancedPhrases, .family, .bodyParts, // ... ✅
        return true
    default:
        return false
    }
}
```

---

## ✅ 免费类别配置

**位置**：`PurchaseManager.swift`

```swift
let freeCategories: Set<WordCategory> = [
    .animals, .colors, .numbers, .fruits, .alphabet, .phrases  // ✅ phrases 已添加
]
```

---

## 🐛 已修复的问题

### 问题：旧的 dailyItems 变量

**错误信息**：
```
warning: initialization of immutable value 'dailyItems' was never used
```

**原因**：
旧的 `dailyItems` 数组定义但没有使用（因为已被 VIP 类别替代）

**修复**：
已删除未使用的 `dailyItems` 定义

---

## 📊 完整数据统计

### 预期生成数量

```
总单字数：234 个

免费版（69 个）：
  - 动物：8
  - 颜色：7
  - 数字：10
  - 水果：8
  - 字母：26
  - 简单句型：10  ← 新增
  
VIP 专属（165 个）：
  - 进阶句型：20  ← 新增
  - 家庭成员：12
  - 身体部位：14
  - 家居物品：15
  - 食物饮料：14
  - 衣服配饰：13
  - 运动活动：13
  - 交通工具：12
  - 天气自然：14
  - 情绪动作：15
  - 学校用品：13
```

---

## 🧪 验证步骤

### 1. 清除旧数据（非常重要！）

```
模拟器：Device → Erase All Content and Settings
或者：长按 App 图标 → 删除 App
```

**为什么必须清除？**
- SwiftData 已缓存旧的类别结构
- 新增的类别和数据不会自动添加到现有数据库
- 必须从头初始化才能生成所有 234 个单字

### 2. 重新运行 App

```bash
Cmd + R
```

### 3. 检查控制台输出

**应该看到**：
```
✅ 已生成 234 個預設單字
```

**如果看到其他数字**（如 204）：
→ 说明数据没有完全重新初始化
→ 必须清除数据重新运行

### 4. 检查首页显示

**免费类别区（应有 6 个）**：
```
✅ 🐶 动物（8 个单字）
✅ 🎨 颜色（7 个单字）
✅ 🔢 数字（10 个单字）
✅ 🍎 水果（8 个单字）
✅ 🔤 字母（26 个字母）
✅ 💬 简单句型（10 个句子）← 应该看到这个
```

**VIP 类别区（应有 11 个）**：
```
🔒 💬 进阶句型（20 个句子）VIP ← 应该看到这个
🔒 👨‍👩‍👧 家庭成员（12 个单字）VIP
🔒 👁️ 身体部位（14 个单字）VIP
... （还有 8 个其他 VIP 类别）
```

### 5. 测试简单句型

**步骤**：
1. 点击 "简单句型" 类别
2. 应进入学习页面
3. 应看到 10 个句子
4. 检查内容：
   - Hello 你好 👋
   - Thank you 謝謝 🙏
   - Good morning 早安 🌅
   - ... 等 10 个

### 6. 测试进阶句型（启用 VIP）

**步骤**：
1. 进入 设置 → 开发者选项
2. 切换到 "付费版"
3. 返回首页
4. 点击 "进阶句型" 类别
5. 应进入学习页面
6. 应看到 20 个句子
7. 检查内容：
   - How are you? 你好嗎？ 😊
   - I am fine 我很好 👍
   - What is this? 這是什麼？ ❓
   - ... 等 20 个

---

## ⚠️ 常见问题

### Q1：为什么首页没有显示句型类别？

**A1**：没有清除旧数据。必须：
```
Device → Erase All Content and Settings
```

### Q2：为什么句型类别是空的（0 个单字）？

**A2**：同样是没有清除旧数据导致的。SwiftData 不会自动更新已存在的数据库结构。

### Q3：控制台显示的单字数不是 234？

**A3**：数据没有完全重新生成。确认：
1. 已清除所有内容和设置
2. 完全关闭模拟器
3. 重新运行 Cmd + R

### Q4：如何确认数据真的生成了？

**A4**：查看控制台日志：
```
✅ 已生成 234 個預設單字  ← 应该是这个数字
```

如果是其他数字（如 204），说明句型数据没有生成。

---

## ✅ 确认清单

在报告"数据正确"之前，请确认：

- [ ] 已清除所有内容和设置
- [ ] 重新运行 App（Cmd + R）
- [ ] 控制台显示 "✅ 已生成 234 個預設單字"
- [ ] 首页显示 6 个免费类别（包括"简单句型"）
- [ ] 首页显示 11 个 VIP 类别（包括"进阶句型"）
- [ ] 点击"简单句型"可以进入，显示 10 个句子
- [ ] 启用 VIP 后，点击"进阶句型"可以进入，显示 20 个句子
- [ ] 所有句子都有正确的 emoji 显示
- [ ] 所有句子都能正确发音

---

## 🎯 数据文件位置总结

| 内容 | 文件 | 位置 | 状态 |
|------|------|------|------|
| 句型类别枚举 | `WordCategory.swift` | 第 14-34 行 | ✅ |
| 类别中文名 | `WordCategory.swift` | 第 39-58 行 | ✅ |
| 类别图标 | `WordCategory.swift` | 第 61-80 行 | ✅ |
| 类别颜色 | `WordCategory.swift` | 第 83-102 行 | ✅ |
| VIP 权限配置 | `WordCategory.swift` | 第 149-157 行 | ✅ |
| 简单句型数据 | `Word.swift` | 第 495-516 行 | ✅ |
| 进阶句型数据 | `Word.swift` | 第 518-562 行 | ✅ |
| 免费类别配置 | `PurchaseManager.swift` | 第 36-38 行 | ✅ |

---

**🎊 所有句型数据都已正确添加！请清除数据重新运行测试！** ✨

