# 👨‍💻 LearnWord 開發者指南

## 📋 目錄
1. [專案架構](#專案架構)
2. [新增功能說明](#新增功能說明)
3. [代碼結構](#代碼結構)
4. [開發環境設置](#開發環境設置)
5. [測試指南](#測試指南)
6. [部署指南](#部署指南)
7. [常見開發任務](#常見開發任務)

---

## 📁 專案架構

### 整體結構
```
learnWord/
├── Models/                           # 資料模型層
│   ├── Word.swift                   # 基礎單字模型
│   ├── WordCategory.swift           # 類別定義
│   ├── LearningProgress.swift       # 學習進度
│   ├── Quiz.swift                   # 測驗相關模型 🆕
│   ├── CustomCategory.swift         # 自訂類別模型 🆕
│   └── WordEmojiMapper.swift        # Emoji 映射工具
│
├── Views/                            # 視圖層
│   ├── HomeView.swift               # 主畫面 ✏️ 已更新
│   ├── CategoryView.swift           # 類別視圖
│   ├── LearningCardView.swift       # 學習卡片
│   ├── SettingsView.swift           # 設置頁面
│   ├── QuizSetupView.swift          # 測驗設置 🆕
│   ├── QuizView.swift               # 測驗進行 🆕
│   ├── QuizResultView.swift         # 測驗結果 🆕
│   └── SubscriptionView.swift       # 訂閱管理 🆕
│
├── Components/                       # UI 組件
│   ├── AnimatedBackground.swift     # 動畫背景
│   ├── CustomButtons.swift          # 自訂按鈕
│   └── RewardAnimations.swift       # 獎勵動畫
│
├── Services/                         # 服務層
│   ├── AudioService.swift           # 音訊服務 ✏️ 已更新
│   ├── QuizManager.swift            # 測驗管理 🆕
│   ├── SubscriptionManager.swift    # 訂閱管理 🆕
│   └── SpeechRecognitionService.swift # 語音識別 🆕
│
├── Utilities/                        # 工具類
│   └── Constants.swift              # 常數配置
│
└── learnWordApp.swift               # App 入口 ✏️ 已更新
```

---

## 🆕 新增功能說明

### 1. 測驗系統

#### 核心文件
- **Quiz.swift**：測驗資料模型
  - `QuizQuestionType`：題型枚舉
  - `QuizDifficulty`：難度枚舉
  - `QuizQuestion`：單一題目結構
  - `QuizResult`：測驗結果模型（SwiftData）
  - `WrongAnswerRecord`：錯題記錄（SwiftData）
  - `QuizConfiguration`：測驗配置結構

- **QuizManager.swift**：測驗邏輯管理
  - 生成測驗題目
  - 處理答題邏輯
  - 計算分數和統計
  - 管理測驗狀態

- **QuizSetupView.swift**：測驗設置界面
  - 類別選擇
  - 難度設置
  - 題數選擇
  - 導航到測驗頁面

- **QuizView.swift**：測驗進行界面
  - 顯示題目
  - 處理用戶選擇
  - 即時反饋
  - 進度追蹤

- **QuizResultView.swift**：結果展示界面
  - 分數展示
  - 星星評分
  - 錯題列表
  - 慶祝動畫

#### 使用流程
```swift
// 1. 創建測驗配置
let config = QuizConfiguration(
    selectedCategories: [.animals, .colors],
    questionCount: 10,
    questionType: nil,  // 混合題型
    difficulty: .easy,
    learnedWordsOnly: false
)

// 2. 創建測驗管理器
let manager = QuizManager(
    configuration: config,
    availableWords: allWords
)

// 3. 開始測驗
manager.startQuiz()

// 4. 提交答案
let isCorrect = manager.submitAnswer(selectedWord)

// 5. 移動到下一題
let hasNext = manager.moveToNextQuestion()

// 6. 獲取結果
let result = manager.getQuizResult()
```

### 2. 付費訂閱系統

#### 核心文件
- **SubscriptionManager.swift**：訂閱管理服務
  - 管理訂閱狀態
  - 處理內購流程
  - 功能權限檢查
  - 本地狀態存儲

- **SubscriptionView.swift**：訂閱界面
  - 功能列表展示
  - 方案選擇
  - 購買流程
  - 恢復購買

- **CustomCategory.swift**：自訂內容模型
  - 自訂類別（SwiftData）
  - 自訂單字（SwiftData）
  - 相關服務方法

#### StoreKit 2 整合
```swift
// 1. 載入產品
await subscriptionManager.loadProducts()

// 2. 購買
let success = await subscriptionManager.purchase(.yearly)

// 3. 恢復購買
await subscriptionManager.restorePurchases()

// 4. 檢查權限
if subscriptionManager.canUseCustomCategories() {
    // 顯示自訂類別功能
}
```

#### 產品 ID 配置
在 App Store Connect 中配置以下產品：
- `com.learnword.premium.monthly` - 月訂閱
- `com.learnword.premium.yearly` - 年訂閱
- `com.learnword.premium.lifetime` - 終身購買

### 3. 發音驗證系統

#### 核心文件
- **SpeechRecognitionService.swift**：語音識別服務
  - 請求權限
  - 錄音處理
  - 語音識別
  - 發音比對
  - 相似度計算

#### 使用流程
```swift
let service = SpeechRecognitionService.shared

// 1. 請求權限
service.requestAuthorization { authorized in
    if authorized {
        // 可以使用
    }
}

// 2. 開始錄音
try await service.startRecording()

// 3. 停止錄音
service.stopRecording()

// 4. 評估發音
let result = service.evaluatePronunciation(
    recognized: service.currentTranscription,
    target: "apple"
)

// 5. 一鍵式檢查（自動化流程）
let result = try await service.checkPronunciation(for: "apple")
```

#### 權限配置
在 `Info.plist` 中添加：
```xml
<key>NSMicrophoneUsageDescription</key>
<string>需要使用麥克風來檢測你的發音</string>

<key>NSSpeechRecognitionUsageDescription</key>
<string>需要使用語音識別來評估發音準確度</string>
```

---

## 💾 SwiftData 模型管理

### 新增模型
已在 `learnWordApp.swift` 中註冊：
```swift
let schema = Schema([
    // 基礎模型
    Word.self,
    LearningProgress.self,
    
    // 測驗功能 🆕
    QuizResult.self,
    WrongAnswerRecord.self,
    
    // 自訂內容 🆕
    CustomCategory.self,
    CustomWord.self
])
```

### 數據遷移
如果需要修改模型結構：
1. 更新模型定義
2. SwiftData 會自動處理輕量級遷移
3. 重大變更需要自定義遷移策略

---

## 🔧 開發環境設置

### 必要工具
- macOS 14.0+（Sonoma）
- Xcode 15.0+
- iOS 17.0+ 模擬器或實體設備

### 初始設置
```bash
# 1. Clone 專案
cd /Users/night/Desktop/learnword/learnWord

# 2. 用 Xcode 打開
open learnWord.xcodeproj

# 3. 選擇模擬器
# Xcode 頂部 > iPhone 15 Pro

# 4. 運行
# Cmd + R
```

### 環境配置
1. **開發模式**：自動啟用測試功能
2. **付費功能測試**：
   ```swift
   #if DEBUG
   SubscriptionManager.shared.enablePremiumForTesting()
   #endif
   ```

---

## 🧪 測試指南

### 測驗功能測試
```swift
// 測試用例 1: 基礎測驗流程
func testBasicQuizFlow() {
    let config = QuizConfiguration.default
    let manager = QuizManager(configuration: config, availableWords: testWords)
    
    manager.startQuiz()
    XCTAssertFalse(manager.isCompleted)
    
    // 提交答案
    let isCorrect = manager.submitAnswer(correctWord)
    XCTAssertTrue(isCorrect)
    
    // 移到下一題
    let hasNext = manager.moveToNextQuestion()
    XCTAssertTrue(hasNext)
}

// 測試用例 2: 分數計算
func testScoreCalculation() {
    // ... 測試代碼
}
```

### 付費功能測試
```swift
// 在開發環境啟用付費功能
SubscriptionManager.shared.enablePremiumForTesting()

// 測試自訂類別
let category = CustomCategory(
    name: "Test Category",
    chineseName: "測試類別"
)
modelContext.insert(category)
```

### UI 測試清單
- [ ] 測驗設置頁面所有控件可用
- [ ] 測驗進行流程順暢
- [ ] 結果頁面數據正確
- [ ] 訂閱頁面顯示正常
- [ ] 發音驗證可以錄音

---

## 📦 部署指南

### App Store 上架流程

#### 1. 配置 App Store Connect
- 創建新版本（2.0）
- 上傳屏幕截圖
- 填寫新功能描述
- 設定內購產品

#### 2. 配置 StoreKit
- 創建訂閱群組
- 添加三個訂閱產品
- 設定價格
- 測試內購

#### 3. 準備資產
- App 圖標（1024x1024）
- 啟動畫面
- 屏幕截圖（多尺寸）
- 預覽視頻（可選）

#### 4. 構建和上傳
```bash
# 1. 設定版本號
# Project > General > Version = 2.0
# Build = 1

# 2. Archive
# Product > Archive

# 3. Validate
# 驗證構建

# 4. Distribute
# 上傳到 App Store
```

#### 5. 提交審核
- 填寫審核信息
- 提供測試帳號（如果需要）
- 提交審核

### 隱私聲明更新
由於新增了語音識別功能，需要更新隱私政策：
- 說明麥克風使用目的
- 語音數據處理方式
- 不會保存錄音

---

## 🛠️ 常見開發任務

### 添加新的測驗題型
```swift
// 1. 在 Quiz.swift 中添加新題型
enum QuizQuestionType: String, Codable {
    case imageToWord = "看圖選單字"
    case soundToImage = "聽音選圖"
    case wordToImage = "單字選圖"
    case spelling = "拼字測驗"
    case newType = "新題型" // 🆕 添加這裡
}

// 2. 在 QuizView.swift 中處理新題型
switch question.type {
case .newType:
    // 實現新題型的 UI
    newTypeView(question)
default:
    // 其他題型
}

// 3. 在 QuizManager.swift 中添加到隨機選擇
let questionType = [.imageToWord, .soundToImage, .wordToImage, .newType].randomElement()
```

### 添加新的訂閱方案
```swift
// 1. 在 SubscriptionManager.swift 中添加
enum SubscriptionPlan: String, CaseIterable {
    case monthly = "com.learnword.premium.monthly"
    case yearly = "com.learnword.premium.yearly"
    case lifetime = "com.learnword.premium.lifetime"
    case familyPlan = "com.learnword.premium.family" // 🆕
    
    var displayName: String {
        switch self {
        case .familyPlan: return "家庭方案"
        // ...
        }
    }
}

// 2. 在 App Store Connect 中創建對應產品
// 3. 在 SubscriptionView.swift 中顯示新方案
```

### 添加新的單字類別
```swift
// 1. 在 WordCategory.swift 中添加
enum WordCategory: String, CaseIterable {
    case animals = "Animals"
    // ... 其他類別
    case weather = "Weather" // 🆕
    
    var chineseName: String {
        switch self {
        case .weather: return "天氣"
        // ...
        }
    }
    
    var themeColor: Color {
        switch self {
        case .weather: return Color(hex: "00D9FF")
        // ...
        }
    }
}

// 2. 在 Word.swift 中添加單字
let weatherWords = [
    ("Sunny", "晴天", "sunny"),
    ("Rainy", "下雨", "rainy"),
    // ...
]
```

### 自訂音效
```swift
// 1. 添加音效文件到專案
// Resources/Sounds/custom_sound.mp3

// 2. 在 AudioService.swift 中添加方法
func playCustomSound() {
    playSound(fileName: "custom_sound")
}

// 3. 在需要的地方調用
audioService.playCustomSound()
```

---

## 🐛 調試技巧

### 常見問題排查

#### 1. 語音識別不工作
```swift
// 檢查權限狀態
print("Auth status: \(SpeechRecognitionService.shared.authorizationStatus)")

// 檢查可用性
print("Available: \(SpeechRecognitionService.shared.isAvailable)")

// 檢查錯誤消息
if let error = SpeechRecognitionService.shared.errorMessage {
    print("Error: \(error)")
}
```

#### 2. 測驗生成失敗
```swift
// 檢查可用單字數量
print("Available words: \(filteredWords.count)")
print("Required: \(configuration.questionCount)")

// 確保有足夠單字
if filteredWords.count < configuration.questionCount {
    print("⚠️ 單字不足")
}
```

#### 3. 內購不工作
```swift
// 檢查產品載入
await subscriptionManager.loadProducts()
print("Products loaded: \(subscriptionManager.availableProducts.count)")

// 檢查狀態消息
if let status = subscriptionManager.statusMessage {
    print("Status: \(status)")
}
```

### 日誌輸出
在開發模式啟用詳細日誌：
```swift
#if DEBUG
let logLevel = "verbose"
#else
let logLevel = "error"
#endif
```

---

## 📈 性能優化

### 測驗系統優化
- 預先生成題目，避免即時計算
- 使用 `@Observable` 而非 `@Published` 減少重繪
- 圖片使用 lazy loading

### 語音識別優化
- 錄音緩衝區大小：1024
- 自動停止錄音：3秒後
- 識別結果緩存

### SwiftData 優化
- 使用索引加速查詢
- 批量插入資料
- 合理使用 Relationship

---

## 🔐 安全考量

### 資料安全
- 訂閱狀態加密存儲
- 不保存語音錄音
- 本地資料加密（考慮中）

### 內購安全
- 使用 StoreKit 2 的內建驗證
- 檢查交易簽名
- 防止越獄設備破解（考慮添加）

---

## 📚 參考資源

### Apple 官方文檔
- [SwiftData Documentation](https://developer.apple.com/documentation/swiftdata)
- [StoreKit 2 Guide](https://developer.apple.com/documentation/storekit)
- [Speech Framework](https://developer.apple.com/documentation/speech)
- [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)

### 相關技術
- SwiftUI
- Combine
- AVFoundation
- Core Data

---

## 🎓 學習建議

### 初級開發者
1. 先理解 SwiftUI 基礎
2. 學習 SwiftData 資料管理
3. 掌握基本的 UI 組件

### 中級開發者
1. 深入理解 StoreKit 2
2. 學習語音識別 API
3. 優化性能和用戶體驗

### 進階開發者
1. 實現 iCloud 同步
2. 添加 Widget 支援
3. 整合 AI/ML 功能

---

## 🤝 貢獻指南

### 代碼規範
- 使用 Swift 官方命名規範
- 添加適當的註釋
- 保持代碼簡潔易讀

### Git 工作流
```bash
# 1. 創建功能分支
git checkout -b feature/new-feature

# 2. 開發和提交
git add .
git commit -m "Add new feature"

# 3. 推送和創建 PR
git push origin feature/new-feature
```

### Pull Request 流程
1. 描述清楚改動內容
2. 添加屏幕截圖（UI 改動）
3. 確保通過所有測試
4. 等待 Code Review

---

## 📞 聯繫方式

- **技術問題**：tech@learnword.app
- **Bug 報告**：GitHub Issues
- **功能建議**：feedback@learnword.app

---

**祝開發順利！** 🚀

*最後更新：2025年11月20日*

