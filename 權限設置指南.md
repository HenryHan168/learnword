# 🔐 發音測驗權限設置指南

## ❗ 問題說明

應用崩潰，錯誤訊息：
```
This app has crashed because it attempted to access privacy-sensitive data 
without a usage description.
```

**原因**：iOS 要求應用在使用麥克風和語音識別功能時，必須在 Info.plist 中說明用途。

## ✅ 解決方案（兩種方法）

---

## 方法一：在 Xcode 中手動添加（推薦）

### 步驟 1: 打開項目設置
1. 在 Xcode 左側導航欄，點擊最上方的 **learnWord** 項目（藍色圖標）
2. 選擇 **TARGETS** 下的 **learnWord**
3. 點擊上方的 **Info** 標籤頁

### 步驟 2: 添加權限說明

#### 添加麥克風權限
1. 在 **Custom iOS Target Properties** 區域
2. 將鼠標移到任意一行，會出現 **+** 按鈕
3. 點擊 **+** 添加新行
4. 在 **Key** 欄位輸入或選擇：
   ```
   Privacy - Microphone Usage Description
   ```
5. 在 **Value** 欄位輸入：
   ```
   LearnWord 需要使用麥克風來檢測你的發音，幫助你學習正確的英文發音。
   ```

#### 添加語音識別權限
1. 再次點擊 **+** 添加新行
2. 在 **Key** 欄位輸入或選擇：
   ```
   Privacy - Speech Recognition Usage Description
   ```
3. 在 **Value** 欄位輸入：
   ```
   LearnWord 需要使用語音識別功能來評估你的發音準確度，給予即時的學習反饋。
   ```

### 步驟 3: 重新編譯運行
```bash
Cmd + Shift + K  # 清理
Cmd + B          # 編譯
Cmd + R          # 運行
```

---

## 方法二：使用已創建的 Info.plist 文件

我已經創建了 `Info.plist` 文件在：
```
/Users/night/Desktop/learnword/learnWord/learnWord/Info.plist
```

### 步驟 1: 將文件添加到 Xcode 項目
1. 在 Xcode 中，右鍵點擊 **learnWord** 資料夾（黃色）
2. 選擇 **Add Files to "learnWord"...**
3. 找到並選擇剛創建的 `Info.plist` 文件
4. 確保勾選：
   - ✅ **Copy items if needed**
   - ✅ **Add to targets: learnWord**
5. 點擊 **Add**

### 步驟 2: 設置為項目的 Info.plist
1. 選擇項目設置（藍色 learnWord 圖標）
2. 選擇 **TARGETS** → **learnWord**
3. 點擊 **Build Settings** 標籤頁
4. 搜尋 "**Info.plist**"
5. 找到 **Info.plist File** 設置
6. 將值設為：`learnWord/Info.plist`

### 步驟 3: 重新編譯運行
```bash
Cmd + Shift + K  # 清理
Cmd + B          # 編譯  
Cmd + R          # 運行
```

---

## 📝 Info.plist 內容

文件已包含以下權限說明：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" 
    "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>NSMicrophoneUsageDescription</key>
    <string>LearnWord 需要使用麥克風來檢測你的發音，幫助你學習正確的英文發音。</string>
    
    <key>NSSpeechRecognitionUsageDescription</key>
    <string>LearnWord 需要使用語音識別功能來評估你的發音準確度，給予即時的學習反饋。</string>
</dict>
</plist>
```

---

## ✅ 驗證設置是否成功

### 1. 檢查權限提示
重新運行應用後，進入發音測驗：
- 應該會彈出麥克風權限請求對話框
- 應該會彈出語音識別權限請求對話框
- 兩個都點擊「**允許**」或「**OK**」

### 2. 測試功能
- 點擊「播放聲音」→ 應該能聽到聲音 ✅
- 點擊「開始錄音」→ 不會崩潰 ✅
- 錄音 3 秒後 → 顯示評分結果 ✅

### 3. 檢查控制台日誌
應該看到：
```
✅ 語音識別權限已授權
🎤 收到播放請求: [單字]
🔊 開始播放: [單字]
```

---

## 🐛 常見問題

### Q1: 添加權限後還是崩潰？
**A:** 確保：
1. Info.plist 文件已正確添加到項目
2. 已清理並重新編譯 (Cmd + Shift + K)
3. 完全關閉應用後重新啟動

### Q2: 找不到 Info.plist File 設置？
**A:** 
1. 在 Build Settings 中確保顯示 "**All**" 而不是 "Basic"
2. 搜尋框輸入 "info"
3. 找到 **Packaging** 區域下的 **Info.plist File**

### Q3: 權限對話框沒有彈出？
**A:**
1. 檢查是否在模擬器上測試（建議用真機）
2. 重置模擬器：Device → Erase All Content and Settings
3. 檢查 Info.plist 中的 Key 名稱是否正確

### Q4: 如何重新請求權限？
**A:**
如果不小心點了「拒絕」：
1. 真機：設定 → LearnWord → 開啟麥克風和語音識別
2. 模擬器：重置模擬器或重新安裝應用

---

## 📱 真機測試注意事項

如果在真機上測試，還需要：

### 1. 設置開發團隊
1. 項目設置 → **Signing & Capabilities**
2. 選擇你的 **Team**
3. Xcode 會自動處理簽名

### 2. 信任開發者證書
第一次在真機運行時：
1. 設定 → 一般 → VPN 與裝置管理
2. 找到你的開發者應用
3. 點擊「信任」

---

## 🎯 完成檢查清單

設置完成後，確認以下事項：

- [ ] Info.plist 文件已添加到項目
- [ ] 包含 NSMicrophoneUsageDescription
- [ ] 包含 NSSpeechRecognitionUsageDescription
- [ ] 已清理並重新編譯
- [ ] 運行時彈出權限請求
- [ ] 已授權麥克風權限
- [ ] 已授權語音識別權限
- [ ] 發音測驗功能正常運作

---

## 📞 需要幫助？

如果按照步驟操作後仍有問題：

1. 截圖分享：
   - Xcode 的 Info 設置頁面
   - 崩潰錯誤訊息
   - 控制台日誌

2. 檢查：
   - Xcode 版本（需要 15.0+）
   - iOS 版本（需要 17.0+）
   - 是否使用模擬器或真機

---

**完成設置後，發音測驗功能就能正常使用了！** 🎤✨

*最後更新：2025年11月20日*

