# 💰 付費功能說明

## 🔊 關於「立即購買」按鈕無反應

### 這是正常的！

在**模擬器環境**下，StoreKit 無法連接到真實的 App Store，所以：
- ✅ 「立即購買」按鈕沒反應是**正常的**
- ✅ StoreKit 錯誤訊息是**預期行為**
- ✅ 無需擔心，這不影響開發和測試

### 為什麼會這樣？

```
Error: No context available to handle authentication request
```

這個錯誤表示：
1. 模擬器沒有登入 Apple ID
2. 無法訪問 App Store 購買系統
3. 這是蘋果的安全限制

---

## ✨ 如何測試付費功能？

### 方式一：開發者開關（推薦）

#### 步驟：

1. **運行 App**
   ```
   Cmd + R
   ```

2. **進入設置**
   ```
   首頁 → 點擊右上角圖表圖標
   ```

3. **查看開發者選項**
   ```
   ┌──────────────────────────────┐
   │ 🔧 開發者選項（測試用）       │
   ├──────────────────────────────┤
   │ 👑 付費狀態: ✅ 已付費        │
   │                              │
   │ [切換到免費版]               │
   │                              │
   │ 💡 在測試環境下，付費功能   │
   │    已自動開啟...              │
   └──────────────────────────────┘
   ```

4. **切換狀態**
   - 點擊「切換到免費版」→ 變成免費版
   - 點擊「切換到付費版」→ 變成付費版
   - 即時生效，無需重啟

5. **測試效果**
   - **免費版**: 30個單字、每日3次發音（測試時100次）
   - **付費版**: 完整單字、無限發音、自訂類別

---

### 方式二：代碼設置

如果需要在代碼層面控制：

```swift
// 在 SubscriptionManager.swift 第74行
var subscriptionStatus: SubscriptionStatus = .lifetime  // 付費版

// 或改為
var subscriptionStatus: SubscriptionStatus = .free  // 免費版
```

修改後記得重新編譯：
```bash
Cmd + Shift + K  (清理)
Cmd + R          (運行)
```

---

## 🎯 付費功能測試清單

### ✅ 已自動開啟的功能

當 `subscriptionStatus = .lifetime` 時：

- ✅ 完整單字庫（58+ 個單字）
- ✅ 無限發音驗證次數
- ✅ 自訂類別與單字
- ✅ 進階測驗模式
- ✅ 錯題本功能
- ✅ 無廣告體驗

### 測試方法

#### 1. 測試自訂類別

```
首頁 → 自訂類別 → 新增類別
應該能正常創建
```

#### 2. 測試無限發音

```
學習任意單字 → 發音測驗
可以無限次測試，不受限制
```

#### 3. 測試完整單字庫

```
首頁 → 查看所有類別
應該看到所有單字，無鎖定標記
```

#### 4. 測試錯題本

```
做測驗 → 答錯幾題 → 回到首頁 → 錯題本
應該看到錯誤記錄
```

---

## 🏪 正式上架後如何測試購買？

### 沙盒測試（App Store Connect）

1. **創建沙盒測試帳號**
   ```
   App Store Connect → 用戶和訪問 → 沙盒測試帳號
   ```

2. **在真機上測試**
   ```
   設置 → App Store → 沙盒帳號
   登入測試帳號
   ```

3. **使用測試帳號購買**
   ```
   測試所有購買流程
   不會實際扣款
   ```

### TestFlight 測試

1. **上傳到 TestFlight**
2. **邀請測試用戶**
3. **真實環境測試**

---

## 🚀 真機測試（推薦）

如果要完全模擬真實環境：

### 步驟：

1. **連接 iPhone/iPad**
   ```
   使用 USB 或 Wi-Fi 連接
   ```

2. **選擇真機**
   ```
   Xcode → 頂部選擇你的設備
   ```

3. **運行**
   ```
   Cmd + R
   ```

4. **測試所有功能**
   ```
   包括語音、音效、購買等
   ```

---

## 🔍 當前狀態檢查

### 如何確認付費功能已開啟？

#### 方法 1：查看設置頁面

```
設置 → 看到「✅ 已付費」標記
```

#### 方法 2：查看控制台

```
運行 App 時，控制台會顯示：
✅ 訂閱狀態已更新：lifetime
```

#### 方法 3：測試功能

```
嘗試創建自訂類別
如果能創建 = 付費功能已啟用
```

---

## 📝 總結

| 環境 | 購買按鈕 | 付費功能 | 測試方法 |
|------|---------|---------|---------|
| **模擬器** | ❌ 無反應 | ✅ 代碼開啟 | 開發者開關 |
| **真機（開發）** | ❌ 無反應 | ✅ 代碼開啟 | 開發者開關 |
| **真機（沙盒）** | ✅ 可測試 | ✅ 測試購買 | 沙盒帳號 |
| **正式上架** | ✅ 可購買 | ✅ 真實購買 | 用戶購買 |

---

## 💡 提示

### 開發階段（現在）

```
✅ 使用代碼設置：subscriptionStatus = .lifetime
✅ 使用開發者開關：在設置中切換
✅ 不需要擔心購買按鈕
✅ 專注於功能開發
```

### 測試階段

```
✅ 使用沙盒帳號
✅ 在真機上測試
✅ TestFlight 分發
```

### 正式上架

```
✅ 移除開發者開關
✅ subscriptionStatus = .free
✅ 用戶真實購買
```

---

## 🎉 現在可以做什麼？

1. **測試語音功能**
   ```
   首頁 → 🔊 語音測試 → 測試各種聲音
   ```

2. **測試付費功能**
   ```
   設置 → 開發者選項 → 切換付費狀態
   ```

3. **測試所有功能**
   ```
   學習、測驗、發音、打卡、錯題本等
   ```

4. **測試自訂類別**
   ```
   首頁 → 自訂類別 → 新增類別和單字
   ```

---

**🎊 付費功能已經完全可用，不需要真的購買！**

如有疑問，隨時告訴我！ 🤗

