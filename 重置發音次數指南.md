# 🔄 重置發音驗證次數指南

## 📋 問題說明

你遇到了「無法使用發音驗證（次數限制）」的提示。這是因為免費版每天有發音驗證次數限制。

## ✅ 已完成的修改

我已經將測試階段的免費次數從 **3 次** 增加到 **100 次**。

## 🚀 如何立即使用（3種方法）

### 方法 1: 刪除並重新安裝（推薦）✅

#### 步驟：
1. **刪除應用**
   - 在模擬器/真機上長按應用圖標
   - 點擊「移除 App」
   - 選擇「刪除 App」

2. **重新編譯運行**
   ```bash
   Cmd + Shift + K  # 清理
   Cmd + R          # 運行
   ```

3. **結果**
   - ✅ 發音驗證次數：0/100
   - ✅ 可以測試 100 次

---

### 方法 2: 清除應用數據（模擬器）✅

#### 步驟：
1. **重置模擬器**
   - Simulator 菜單 → Device → Erase All Content and Settings
   
2. **重新運行**
   ```bash
   Cmd + R
   ```

3. **結果**
   - ✅ 所有數據清空
   - ✅ 次數重置為 0/100

---

### 方法 3: 手動清除 UserDefaults（終端）✅

#### 步驟：
1. **找到應用的 Bundle ID**
   ```bash
   # 在 Xcode 中查看
   # Project Settings → General → Bundle Identifier
   # 例如：com.yourname.learnWord
   ```

2. **清除 UserDefaults**
   ```bash
   # 如果是模擬器
   xcrun simctl privacy booted reset all com.yourname.learnWord
   
   # 或者刪除應用容器
   xcrun simctl uninstall booted com.yourname.learnWord
   ```

3. **重新運行應用**

---

## 🔍 驗證是否成功

運行應用後，點擊「開始錄音」，查看控制台：

### 成功的輸出：
```
🔍 發音驗證檢查: 已使用 0/100 次, 可使用=true
🖱️ 點擊了開始錄音按鈕
✅ 開始執行錄音...
🎤 收到錄音請求: Cherry
```

### 失敗的輸出：
```
🔍 發音驗證檢查: 已使用 3/100 次, 可使用=false
❌ 無法使用發音驗證（次數限制）
```

如果還是顯示「已使用 3/100」，說明舊數據還在，請刪除應用重新安裝。

---

## 📊 當前設置

### 開發階段（現在）
```swift
免費版發音驗證次數：100 次/天
```

### 正式上線時
```swift
免費版發音驗證次數：3 次/天
付費版：無限次
```

---

## 🛠️ 開發階段建議

### 1. 使用模擬器測試
- 優點：可以快速重置
- 缺點：語音識別可能不準確

### 2. 使用真機測試
- 優點：語音識別更準確
- 缺點：需要連接設備

### 3. 測試付費功能
需要：
- App Store Connect 設置
- 沙盒測試帳號
- 配置產品 ID

---

## 💡 快速測試流程

```bash
# 1. 停止應用
# 2. 刪除應用
# 3. 清理並重新運行
Cmd + Shift + K
Cmd + R

# 4. 進入發音測驗
# 5. 查看日誌確認次數：0/100
```

---

## 🐛 常見問題

### Q1: 刪除應用後還是顯示已使用 3 次？
**A:** 
- 確認應用完全刪除了
- 清理 Xcode 的 DerivedData：
  ```bash
  rm -rf ~/Library/Developer/Xcode/DerivedData
  ```

### Q2: 如何查看當前使用次數？
**A:** 
點擊「開始錄音」按鈕，查看控制台輸出：
```
🔍 發音驗證檢查: 已使用 X/100 次
```

### Q3: 能否在代碼中自動重置？
**A:** 
可以，但不建議在生產代碼中添加。
目前的方案（刪除重裝）更安全。

### Q4: 上線前需要改什麼？
**A:** 
在 `SubscriptionManager.swift` 中：
```swift
// 改回正式版的次數
let freeDailyPronunciationAttempts: Int = 3
```

---

## 📝 注意事項

### ⚠️ 開發階段
- ✅ 使用 100 次/天 方便測試
- ✅ 可以隨時刪除重裝重置
- ✅ 不需要付費功能

### ⚠️ 上線前
- ❗ 記得改回 3 次/天
- ❗ 設置 StoreKit 產品
- ❗ 測試付費流程

---

## 🎯 立即開始

**最快的方法：**

1. 長按應用圖標 → 刪除 App
2. Xcode 中按 `Cmd + R`
3. 等待安裝完成
4. 進入發音測驗
5. 開始測試！

**現在你有 100 次機會測試發音功能了！** 🎤✨

---

*最後更新：2025年11月25日*

